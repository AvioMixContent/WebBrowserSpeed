<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Web Browser Speed – Speedometer 3.1</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        /* Base body styling */
        body {
            font-family: 'Inter', sans-serif;
            background-color: #ffffff; /* White background */
        }
        /* Styling for individual browser cards */
        .browser-card {
            background-color: #ffffff;
            border-radius: 1rem; /* More rounded corners */
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.05); /* Softer shadow */
            padding: 1rem; /* Reduced padding for lower height */
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            /* height: 100%; Removed to allow height to shrink */
            border-width: 2px; /* Add border width */
            transition: all 0.3s ease-in-out; /* Smooth transition for hover effects */
        }
        /* Hover effect for browser cards */
        .browser-card:hover {
            transform: scale(1.03); /* Slight enlargement on hover */
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.1); /* More pronounced shadow on hover */
        }
        /* Container for vertical browser performance bars */
        .browser-bar-vertical-container {
            width: 40px; /* Width of the vertical bar */
            height: 120px; /* Reduced by 20% from 150px (150 * 0.8 = 120) */
            background-color: #e2e8f0; /* Lighter gray for bar background */
            border-radius: 0.5rem; /* Rounded corners for bar container */
            overflow: hidden;
            display: flex;
            align-items: flex-end; /* Bars grow from bottom */
            margin-bottom: 0.75rem; /* Reduced margin-bottom */
        }
        /* Styling for the vertical browser performance bars */
        .browser-bar-vertical {
            width: 100%;
            /* Default color, overridden by specific browser classes */
            background-color: #6366f1;
            border-radius: 0.5rem; /* Rounded corners for the bar itself */
            transition: height 0.7s ease-out; /* Smooth transition for height changes */
        }
        /* Colors corresponding to browser logos for bars */
        .browser-bar-vertical.edge { background-color: #00A3A3; } /* Teal for Edge */
        .browser-bar-vertical.chrome { background-color: #facc15; } /* Yellow from Chrome logo */
        .browser-bar-vertical.firefox { background-color: #f97316; } /* Orange from Firefox logo */
        .browser-bar-vertical.brave { background-color: #f59e0b; } /* Amber/Orange from Brave logo */
        .browser-bar-vertical.opera { background-color: #ef4444; } /* Red from Opera logo */
        .browser-bar-vertical.safari { background-color: #3b82f6; } /* Blue from Safari logo (used if score available) */
        .browser-bar-vertical.safari-na { background-color: #94a3b8; } /* Gray for Safari (Not available) */

        /* Maximum score for scaling is 50 */
        .h-edge { height: calc((35.5 / 50) * 100%); }
        .h-chrome { height: calc((35.1 / 50) * 100%); }
        .h-firefox { height: calc((30.5 / 50) * 100%); }
        .h-brave { height: calc((35.3 / 50) * 100%); }
        .h-opera { height: calc((34.8 / 50) * 100%); }
        .h-safari { height: calc((34.5 / 50) * 100%); } /* Minimum height for Safari visibility */

        /* Border colors of tiles according to graphs */
        .browser-card.border-edge { border-color: #00A3A3; }
        .browser-card.border-chrome { border-color: #facc15; }
        .browser-card.border-firefox { border-color: #f97316; }
        .browser-card.border-brave { border-color: #f59e0b; }
        .browser-card.border-opera { border-color: #ef4444; }
        .browser-card.border-safari-na { border-color: #94a3b8; }
        .browser-card.border-safari { border-color: #3b82f6; } /* Added Safari border color */

        /* Gradient for heading text - changed to top to bottom */
        .text-gradient {
            background-image: linear-gradient(to bottom, #22c55e, #facc15, #f97316, #ef4444);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }
    </style>
</head>
<body class="flex flex-col items-center min-h-screen">

    <header class="w-full max-w-5xl mx-auto bg-black h-8 flex items-center justify-end px-4 shadow-lg rounded-b-xl">
        <div class="flex items-center space-x-2">
            <a href="mailto:info@example.com" class="text-white flex items-center hover:text-gray-300 transition-colors text-xs">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-mail mr-1"><rect width="20" height="16" x="2" y="4" rx="2"/><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/></svg>
                info@example.com
            </a>
            <a href="https://www.facebook.com/yourpage" target="_blank" class="text-white hover:text-gray-300 transition-colors">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-facebook"><path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z"/></svg>
            </a>
            <a href="https://www.instagram.com/yourprofile" target="_blank" class="text-white hover:text-gray-300 transition-colors">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-instagram"><rect width="20" height="20" x="2" y="2" rx="5" ry="5"/><path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"/><line x1="17.5" x2="17.51" y1="6.5" y2="6.5"/></svg>
            </a>
        </div>
    </header>

    <div class="w-full max-w-5xl bg-white p-6 sm:p-8 md:p-10 rounded-xl shadow-2xl">
        <div class="flex flex-col sm:flex-row sm:items-center justify-between mb-6 sm:mb-8">
            <div class="flex items-center mb-4 sm:mb-0">
                <svg xmlns="http://www.w3.org/2000/svg" width="96" height="96" viewBox="0 0 24 24" fill="none" stroke="url(#logo-gradient)" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-gauge mr-6">
                    <defs>
                        <linearGradient id="logo-gradient" x1="0%" y1="0%" x2="100%" y2="0%">
                            <stop offset="0%" style="stop-color:#22c55e;stop-opacity:1" /> <stop offset="33%" style="stop-color:#facc15;stop-opacity:1" /> <stop offset="66%" style="stop-color:#f97316;stop-opacity:1" /> <stop offset="100%" style="stop-color:#ef4444;stop-opacity:1" /> </linearGradient>
                    </defs>
                    <path d="m12 14 4-4"/>
                    <path d="M3.34 19.06a10 10 0 1 1 17.32 0"/>
                </svg>
                <div class="flex flex-col items-start">
                    <h1 class="text-base sm:text-xl font-bold leading-tight text-gradient">Browsers</h1>
                    <h1 class="text-base sm:text-xl font-bold leading-tight text-gradient">Performance</h1>
                    <h1 class="text-base sm:text-xl font-bold leading-tight text-gradient">Overview</h1>
                </div>
            </div>
        </div>

        <nav class="w-full mb-6 sm:mb-8">
            <ul class="flex flex-wrap justify-center gap-2">
                <li class="flex-1 min-w-0">
                    <button class="w-full px-2 py-2 rounded-lg bg-white text-gray-800 text-xs font-semibold border-4 transition-all duration-300 ease-in-out shadow-sm text-center hover:scale-105 hover:shadow-xl" onclick="selectConfiguration('config1')">i7 13980HX<br>RTX 4090 laptop<br>32 GB DDR5 4800 MHz</button>
                </li>
                <li class="flex-1 min-w-0">
                    <button class="w-full px-2 py-2 rounded-lg bg-white text-gray-800 text-xs font-semibold border-4 transition-all duration-300 ease-in-out shadow-sm text-center hover:scale-105 hover:shadow-xl" onclick="selectConfiguration('config2')">7800X3D<br>RTX 4080 Super<br>32 GB DDR5 6000 MHz CL 28</button>
                </li>
                <li class="flex-1 min-w-0">
                    <button class="w-full px-2 py-2 rounded-lg bg-white text-gray-800 text-xs font-semibold border-4 transition-all duration-300 ease-in-out shadow-sm text-center hover:scale-105 hover:shadow-xl" onclick="selectConfiguration('config3')">i5 10400<br>-<br>16 GB DDR5 2666 MHz</button>
                </li>
                <li class="flex-1 min-w-0">
                    <button class="w-full px-2 py-2 rounded-lg bg-white text-gray-800 text-xs font-semibold border-4 transition-all duration-300 ease-in-out shadow-sm text-center hover:scale-105 hover:shadow-xl" onclick="selectConfiguration('config4')">i7 10750H<br>RTX 2070 Super laptop<br>16 GB DDR5 2666 MHz</button>
                </li>
            </ul>
        </nav>
        <div class="relative w-full h-40 rounded-xl overflow-hidden shadow-lg mb-8">
            <img src="https://placehold.co/800x160/333333/FFFFFF?text=Optimize+your+online+experience" alt="Abstract speed background" class="absolute inset-0 w-full h-full object-cover filter brightness-75" loading="lazy">
            <div class="relative z-10 flex flex-col items-center justify-center h-full p-4 text-white">
                <h2 class="text-3xl font-bold mb-2 drop-shadow-md">Discover which browser is the fastest!</h2>
                <p class="text-lg text-gray-100 drop-shadow-sm">Our tests reveal real performance.</p>
            </div>
        </div>

        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 sm:gap-8">
            <div class="browser-card border-edge">
                <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/9/98/Microsoft_Edge_logo_%282019%29.svg/48px-Microsoft_Edge_logo_%282019%29.svg.png" alt="Edge browser logo" class="w-12 h-12 mb-2" onerror="this.onerror=null;this.src='https://placehold.co/48x48/cccccc/000000?text=Edge';" loading="lazy"/>
                <p class="font-semibold text-xl text-gray-700 mb-2">Edge</p> <div class="browser-bar-vertical-container">
                    <div class="browser-bar-vertical edge"></div>
                </div>
                <span class="font-bold text-2xl text-gray-800"></span>
                <span class="text-sm text-gray-500">points</span>
            </div>

            <div class="browser-card border-chrome">
                <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/a/a5/Google_Chrome_icon_%28September_2014%29.svg/48px-Google_Chrome_icon_%28September_2014%29.svg.png" alt="Chrome browser logo" class="w-12 h-12 mb-2" onerror="this.onerror=null;this.src='https://placehold.co/48x48/cccccc/000000?text=Chrome';" loading="lazy"/>
                <p class="font-semibold text-xl text-gray-700 mb-2">Chrome</p> <div class="browser-bar-vertical-container">
                    <div class="browser-bar-vertical chrome"></div>
                </div>
                <span class="font-bold text-2xl text-gray-800"></span>
                <span class="text-sm text-gray-500">points</span>
            </div>

            <div class="browser-card border-brave">
                <img src="https://upload.wikimedia.org/wikipedia/commons/5/51/Brave_icon_lionface.png" alt="Brave browser logo" class="w-auto h-12 mb-2" onerror="this.onerror=null;this.src='https://placehold.co/48x48/cccccc/000000?text=Brave';" loading="lazy"/>
                <p class="font-semibold text-xl text-gray-700 mb-2">Brave</p> <div class="browser-bar-vertical-container">
                    <div class="browser-bar-vertical brave"></div>
                </div>
                <span class="font-bold text-2xl text-gray-800"></span>
                <span class="text-sm text-gray-500">points</span>
            </div>

            <div class="browser-card border-opera">
                <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/4/49/Opera_2015_icon.svg/48px-Opera_2015_icon.svg.png" alt="Opera browser logo" class="w-12 h-12 mb-2" onerror="this.onerror=null;this.src='https://placehold.co/48x48/cccccc/000000?text=Opera';" loading="lazy"/>
                <p class="font-semibold text-xl text-gray-700 mb-2">Opera</p> <div class="browser-bar-vertical-container">
                    <div class="browser-bar-vertical opera"></div>
                </div>
                <span class="font-bold text-2xl text-gray-800"></span>
                <span class="text-sm text-gray-500">points</span>
            </div>

            <div class="browser-card border-firefox">
                <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/a/a0/Firefox_logo%2C_2019.svg/48px-Firefox_logo%2C_2019.svg.png" alt="Firefox browser logo" class="w-12 h-12 mb-2" onerror="this.onerror=null;this.src='https://placehold.co/48x48/cccccc/000000?text=Firefox';" loading="lazy"/>
                <p class="font-semibold text-xl text-gray-700 mb-2">Firefox</p> <div class="browser-bar-vertical-container">
                    <div class="browser-bar-vertical firefox"></div>
                </div>
                <span class="font-bold text-2xl text-gray-800"></span>
                <span class="text-sm text-gray-500">points</span>
            </div>

            <div class="browser-card border-safari-na">
                <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/5/52/Safari_browser_logo.svg/48px-Safari_browser_logo.svg.png" alt="Safari browser logo" class="w-12 h-12 mb-2" onerror="this.onerror=null;this.src='https://placehold.co/48x48/cccccc/000000?text=Safari';" loading="lazy"/>
                <p class="font-semibold text-xl text-gray-700 mb-2">Safari</p> <div class="browser-bar-vertical-container">
                    <div class="browser-bar-vertical safari-na"></div>
                </div>
                <span class="font-bold text-2xl text-gray-800"></span>
                <span class="text-sm text-gray-500">(Not available / Not tested)</span>
            </div>
        </div>

        <p class="text-center text-gray-500 text-sm mt-10">
            Tested on 26.5.2025. Browser versions are always the latest as of the testing date.
        </p>
    </div>

    <footer class="w-full max-w-5xl mx-auto bg-black text-white text-center py-4 shadow-lg rounded-t-xl">
        <p class="text-sm">&copy; 2025 Web Browser Speed. All rights reserved.</p>
    </footer>

    <script>
        // Data for each PC configuration, including browser performance scores
        const configurations = {
            'config1': { // i7 13980HX, RTX 4090 laptop, 32 GB DDR5 4800 MHz
                edge: 35.5,
                chrome: 35.2,
                firefox: 26.0,
                brave: 33.6,
                opera: 32.2,
                safari: 0.0
            },
            'config2': { // 7800X3D, RTX 4080 Super, 32 GB RAM DDR5 6000 MHz @ CL 28
                edge: 34.1,
                chrome: 34.8,
                firefox: 28.5,
                brave: 34.9,
                opera: 33.5,
                safari: 0.0
            },
            'config3': { // i5 10400, 16 GB DDR5 2666 MHz
                edge: 18.3,
                chrome: 17.3,
                firefox: 14.9,
                brave: 17.1,
                opera: 16.2,
                safari: 0.0
            },
            'config4': { // i7 10750H, RTX 2070 Super laptop, 16 GB DDR5 2666 MHz
                edge: 19.0,
                chrome: 18.5,
                firefox: 15.5,
                brave: 18.7,
                opera: 17.8,
                safari: 0.0
            }
        };

        /**
         * Updates the displayed browser speeds based on the provided data.
         * @param {object} data - An object containing scores for each browser.
         */
        function updateBrowserSpeeds(data) {
            const maxScore = 50; // Maximum score for scaling the bars

            // Update Edge browser data
            document.querySelector('.browser-bar-vertical.edge').style.height = `calc((${data.edge} / ${maxScore}) * 100%)`;
            document.querySelector('.browser-card.border-edge span:first-of-type').textContent = data.edge.toFixed(1);

            // Update Chrome browser data
            document.querySelector('.browser-bar-vertical.chrome').style.height = `calc((${data.chrome} / ${maxScore}) * 100%)`;
            document.querySelector('.browser-card.border-chrome span:first-of-type').textContent = data.chrome.toFixed(1);

            // Update Firefox browser data
            document.querySelector('.browser-bar-vertical.firefox').style.height = `calc((${data.firefox} / ${maxScore}) * 100%)`;
            document.querySelector('.browser-card.border-firefox span:first-of-type').textContent = data.firefox.toFixed(1);

            // Update Brave browser data
            document.querySelector('.browser-bar-vertical.brave').style.height = `calc((${data.brave} / ${maxScore}) * 100%)`;
            document.querySelector('.browser-card.border-brave span:first-of-type').textContent = data.brave.toFixed(1);

            // Update Opera browser data
            document.querySelector('.browser-bar-vertical.opera').style.height = `calc((${data.opera} / ${maxScore}) * 100%)`;
            document.querySelector('.browser-card.border-opera span:first-of-type').textContent = data.opera.toFixed(1);

            // Select Safari elements robustly to avoid errors if classes change
            const safariBar = document.querySelector('.browser-card.border-safari-na .browser-bar-vertical.safari-na') || document.querySelector('.browser-card.border-safari .browser-bar-vertical.safari');
            const safariCard = document.querySelector('.browser-card.border-safari-na') || document.querySelector('.browser-card.border-safari');
            const safariScoreSpan = safariCard ? safariCard.querySelector('span:first-of-type') : null;
            const safariTextSpan = safariCard ? safariCard.querySelector('span:last-of-type') : null;

            // Update Safari browser data based on availability
            if (safariBar && safariScoreSpan && safariTextSpan && safariCard) {
                if (data.safari > 0) {
                    safariBar.classList.remove('safari-na');
                    safariBar.classList.add('safari');
                    safariBar.style.height = `calc((${data.safari} / ${maxScore}) * 100%)`;
                    safariScoreSpan.textContent = data.safari.toFixed(1);
                    safariTextSpan.textContent = 'points';
                    safariCard.classList.remove('border-safari-na');
                    safariCard.classList.add('border-safari');
                } else {
                    safariBar.classList.remove('safari');
                    safariBar.classList.add('safari-na');
                    safariBar.style.height = `calc((${data.safari} / ${maxScore}) * 100%)`;
                    safariScoreSpan.textContent = data.safari.toFixed(1);
                    safariTextSpan.textContent = '(Not available / Not tested)';
                    safariCard.classList.remove('border-safari');
                    safariCard.classList.add('border-safari-na');
                }
            } else {
                console.error("One or more Safari elements not found for update.");
            }
        }

        /**
         * Handles the selection of a PC configuration and updates the UI accordingly.
         * @param {string} configKey - The key of the selected configuration (e.g., 'config1').
         */
        function selectConfiguration(configKey) {
            console.log("Selected PC configuration:", configKey);

            // Reset all buttons to their default (non-active) state and apply correct border colors
            document.querySelectorAll('nav button').forEach(button => {
                button.classList.remove('bg-blue-500', 'bg-green-500', 'text-white'); // Remove active styles and fill colors
                button.classList.add('bg-white', 'text-gray-800'); // Apply default text/background

                // Remove any specific border colors
                button.classList.remove('border-green-500', 'border-blue-500');

                // Reapply the correct border color based on its configKey
                const currentButtonConfigKey = button.getAttribute('onclick').match(/'([^']+)'/)[1];
                if (currentButtonConfigKey === 'config2') {
                    button.classList.add('border-green-500');
                } else {
                    button.classList.add('border-blue-500');
                }
            });

            // Apply active styles to the clicked button
            const selectedButton = document.querySelector(`button[onclick="selectConfiguration('${configKey}')"]`);
            if (selectedButton) {
                selectedButton.classList.remove('bg-white', 'text-gray-800'); // Remove default text/background
                selectedButton.classList.add('text-white'); // Apply active text color

                // Apply fill color based on its configKey
                if (configKey === 'config2') {
                    selectedButton.classList.add('bg-green-500');
                } else {
                    selectedButton.classList.add('bg-blue-500');
                }
            }

            // Update the browser speeds based on the selected configuration data
            if (configurations[configKey]) {
                updateBrowserSpeeds(configurations[configKey]);
            }
        }

        // Initial setup on page load
        document.addEventListener('DOMContentLoaded', () => {
            // Apply correct borders and initial state to all buttons
            document.querySelectorAll('nav button').forEach(button => {
                button.classList.add('border-4'); // Make borders bold
                const buttonConfigKey = button.getAttribute('onclick').match(/'([^']+)'/)[1];
                if (buttonConfigKey === 'config2') {
                    button.classList.add('border-green-500');
                } else {
                    button.classList.add('border-blue-500');
                }
            });

            // Then simulate click on the first button to set its active state
            const firstButton = document.querySelector('nav button');
            if (firstButton) {
                const onclickAttr = firstButton.getAttribute('onclick');
                const configKeyMatch = onclickAttr.match(/'([^']+)'/);
                if (configKeyMatch && configKeyMatch[1]) {
                    selectConfiguration(configKeyMatch[1]);
                }
            }
        });
    </script>
</body>
</html>
